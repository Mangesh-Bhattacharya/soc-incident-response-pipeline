# Optional. Leave unset to run the dashboard in demo mode (canned scenarios,
# no backend required). Set this to point the dashboard at the FastAPI
# backend in ../api/ instead — see ../requirements-api.txt.
VITE_API_BASE_URL=http://localhost:8000
